<!DOCTYPE html>
<html lang="en">
    <%- include("./partials/head.ejs") %>
<body>
    <div class="container-fluid">
        <div class="row">
            <div class="col-sm-2">
                <%- include("./partials/nav.ejs") %>
            </div>
            <div class="col-sm-4">
                <table class="table bordered table-hover table-dark">
                    <caption><%= title %></caption>
                    <thead>
                        <h1 class="rounded bg-success text-white border border-primary mt-2 pt-2 pb-2 text-center"><%= title %></h1>
                    </thead>
                    <tbody>
                        <tr>
                            <th scope="row">Track Name</th>
                            <td class="bg-info"><%= track.name %></td>
                        </tr>
                        <tr>
                            <th scope="row">Album Name</th>
                            <td class="bg-info"><a class="text-white" href=<%= "/catalog/album/"+track.album._id %>><%= track.album.name %></a></td>
                        </tr>
                        <tr>
                            <th scope="row">Genre Name</th>
                            <td class="bg-info">
                                <% track.genre?.forEach(item => { %>
                                    <span><a class="text-white" href=<%= "/catalog/genre/"+item.id.toString() %>><%= item.name %></a></span>
                                <% }) %>
                            </td>
                        </tr>
                    </tbody>
                </table>
                <hr>
                <hr>
                <nav class="nav nav-pills row nav-fills">
                    <a class="col nav-item btn btn-lg btn-info" href=<%= track.url+"/update" %>>Update</a>
                    <a class="col nav-item btn btn-lg btn-danger" href=<%= track.url+"/delete" %>>Delete</a>
                </nav>
            </div>
        </div>
    </div>
</body>
</html>