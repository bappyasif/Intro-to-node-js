<!DOCTYPE html>
<html lang="en">
    <%- include("./partials/head.ejs") %>
<body>
    <div class="container-fluid">
        <div class="row">
            <div class="col-sm-2">
                <%- include("./partials/nav.ejs") %>
            </div>
            <div class="col-sm-4">
                <h1 class="text-primary"><%= title %> Of <%= album.name %></h1>
                <div class="card text-center">
                    <h2 class="text-info" class="card-title text-danger">Title: <%= album.name %></h2>
                    <div class="card-body">
                        <h2 class="text-info">Artist: <span class="text-danger"><%= album.artistName %></span></h2>
                        <h2 class="text-info">Genre: <span class="text-danger"><%= album.album_genre %></span></h2>
                        <h2 class="text-info">Released: <span class="text-danger"><%= album.released_date_format %></span></h2>
                        <h2 class="text-info">Description: </h2>
                        <p class="card-text text-primary"><%= album.description %></p>
                        <h2 class="text-info">Price: <span class="text-danger"><%= album.price %></span></h2>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>
</html>